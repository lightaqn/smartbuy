import React from "react";
import { MainPage } from "@/components";

type Props = {};

const Page = (props: Props) => {
  return (
    <main className="">
      <MainPage />
    </main>
  );
};

export default Page;
